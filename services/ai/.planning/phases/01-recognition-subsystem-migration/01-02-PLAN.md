---
phase: 01-recognition-subsystem-migration
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - services/ai/recognition/embedding_service.py
  - services/ai/recognition/ranking_service.py
  - services/backend/api/v1/endpoints/ai_events.py
  - services/backend/api/v1/endpoints/missing_persons.py
autonomous: true
requirements:
  - RECG-01
  - RECG-02
  - RECG-03
  - RECG-04
must_haves:
  truths:
    - "Import statements are updated to reference services.ai.recognition"
    - "FastAPI backend server starts up without import errors"
  artifacts:
    - path: "services/backend/api/v1/endpoints/ai_events.py"
      provides: "FastAPI AI events route"
    - path: "services/backend/api/v1/endpoints/missing_persons.py"
      provides: "FastAPI missing persons route"
  key_links:
    - from: "services/backend/api/v1/endpoints/ai_events.py"
      to: "services/ai/recognition/embedding_service.py"
      via: "import generate_embedding"
      pattern: "from services\\.ai\\.recognition\\.embedding_service import generate_embedding"
    - from: "services/backend/api/v1/endpoints/ai_events.py"
      to: "services/ai/recognition/ranking_service.py"
      via: "import get_best_match"
      pattern: "from services\\.ai\\.recognition\\.ranking_service import get_best_match"
    - from: "services/backend/api/v1/endpoints/missing_persons.py"
      to: "services/ai/recognition/embedding_service.py"
      via: "import generate_embedding"
      pattern: "from services\\.ai\\.recognition\\.embedding_service import generate_embedding"
---

<objective>
Update import statements across migrated modules and calling backend API routes to restore full system functionality under the new folder structure.

Purpose: Re-link the Python namespaces so backend endpoints and internal modules connect to the new recognition package.
Output: Updated imports in migrated modules and backend routers.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-recognition-subsystem-migration/01-CONTEXT.md
@.planning/phases/01-recognition-subsystem-migration/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Fix internal imports in recognition modules</name>
  <files>
    services/ai/recognition/embedding_service.py,
    services/ai/recognition/ranking_service.py
  </files>
  <read_first>
    services/ai/recognition/embedding_service.py,
    services/ai/recognition/ranking_service.py
  </read_first>
  <action>
    In `services/ai/recognition/embedding_service.py`:
    Replace the line `from services.ai.core.deepface_service import DeepFaceConfig` with `from services.ai.recognition.deepface_service import DeepFaceConfig`.
    
    In `services/ai/recognition/ranking_service.py`:
    Replace the line `from services.ai.core.similarity_service import calculate_cosine_similarity` with `from services.ai.recognition.similarity_service import calculate_cosine_similarity`.
  </action>
  <verify>
    python -m py_compile services/ai/recognition/embedding_service.py services/ai/recognition/ranking_service.py
  </verify>
  <done>
    Internal imports corrected and code compiles without errors.
  </done>
  <acceptance_criteria>
    - services/ai/recognition/embedding_service.py contains "from services.ai.recognition.deepface_service import DeepFaceConfig"
    - services/ai/recognition/ranking_service.py contains "from services.ai.recognition.similarity_service import calculate_cosine_similarity"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Fix backend endpoint imports</name>
  <files>
    services/backend/api/v1/endpoints/ai_events.py,
    services/backend/api/v1/endpoints/missing_persons.py
  </files>
  <read_first>
    services/backend/api/v1/endpoints/ai_events.py,
    services/backend/api/v1/endpoints/missing_persons.py
  </read_first>
  <action>
    In `services/backend/api/v1/endpoints/ai_events.py`:
    Replace:
    - `from services.ai.core.embedding_service import generate_embedding` with `from services.ai.recognition.embedding_service import generate_embedding`
    - `from services.ai.core.ranking_service import get_best_match` with `from services.ai.recognition.ranking_service import get_best_match`

    In `services/backend/api/v1/endpoints/missing_persons.py`:
    Replace:
    - `from services.ai.core.embedding_service import generate_embedding` with `from services.ai.recognition.embedding_service import generate_embedding`
  </action>
  <verify>
    python -m py_compile services/backend/api/v1/endpoints/ai_events.py services/backend/api/v1/endpoints/missing_persons.py
  </verify>
  <done>
    Backend router files compile successfully.
  </done>
  <acceptance_criteria>
    - services/backend/api/v1/endpoints/ai_events.py references services.ai.recognition
    - services/backend/api/v1/endpoints/missing_persons.py references services.ai.recognition
  </acceptance_criteria>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Confirm no references to `services.ai.core` exist in `services/ai/recognition/` or `services/backend/`.
- [ ] Confirm python compilation succeeds for all edited files.
</verification>

<success_criteria>
- Imports fully updated.
- Compilation check passes successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/01-recognition-subsystem-migration/01-02-SUMMARY.md`
</output>
