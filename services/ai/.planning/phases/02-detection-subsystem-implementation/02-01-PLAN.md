---
phase: 02-detection-subsystem-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/ai/detection/__init__.py
  - services/ai/detection/video_service.py
  - services/ai/detection/frame_extractor.py
  - services/ai/tests/__init__.py
  - services/ai/tests/test_detection.py
autonomous: true
requirements:
  - DETC-01
  - DETC-02
  - DETC-06
must_haves:
  truths:
    - "video_service.py and frame_extractor.py exist in services/ai/detection/"
    - "Frame extraction utilizes OpenCV's cap.grab() loop for frame skipping"
    - "Unit tests exist for video ingestion and frame extraction"
  artifacts:
    - path: "services/ai/detection/video_service.py"
      provides: "Video properties retrieval and Validation"
    - path: "services/ai/detection/frame_extractor.py"
      provides: "OpenCV frame extraction with skip interval"
    - path: "services/ai/tests/test_detection.py"
      provides: "Automated unit tests for detection modules"
---

<objective>
Implement video file ingestion and frame extraction logic using OpenCV with configurable frame-skipping optimizations. Set up the testing folder structure and stubs as Wave 0 dependencies.

Purpose: Ingest video inputs and extract key frames efficiently to save CPU resource load.
Output: Ingestion and frame-skipping extraction modules.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-detection-subsystem-implementation/02-CONTEXT.md
@.planning/phases/02-detection-subsystem-implementation/02-RESEARCH.md
@.planning/phases/02-detection-subsystem-implementation/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Create package structure and test stubs (Wave 0)</name>
  <files>
    services/ai/detection/__init__.py,
    services/ai/tests/__init__.py,
    services/ai/tests/test_detection.py
  </files>
  <read_first>
    .planning/phases/02-detection-subsystem-implementation/02-VALIDATION.md
  </read_first>
  <action>
    Create the directory `services/ai/detection/` if it doesn't exist.
    Create a blank `__init__.py` file in `services/ai/detection/` to make it a package.
    Create the directory `services/ai/tests/` if it doesn't exist.
    Create a blank `__init__.py` in `services/ai/tests/`.
    Create `services/ai/tests/test_detection.py` containing a boilerplate unittest suite with test case stubs for VideoService, FrameExtractor, FaceDetection, FaceCropper, and Preprocessing.
  </action>
  <verify>
    services\venv\Scripts\python.exe -m unittest services/ai/tests/test_detection.py
  </verify>
  <done>
    Directory structures and test stubs successfully created.
  </done>
  <acceptance_criteria>
    - services/ai/detection/__init__.py exists
    - services/ai/tests/test_detection.py exists and runs without errors (even if stubs are empty or pass by default)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Implement Video Service</name>
  <files>
    services/ai/detection/video_service.py
  </files>
  <read_first>
    services/ai/tests/test_detection.py
  </read_first>
  <action>
    Create and implement `services/ai/detection/video_service.py` to wrap video file validation and metadata loading:
    - Define a `VideoService` class or module.
    - Implement a function `get_video_properties(video_path: str) -> dict` that checks if the video file exists and uses OpenCV (`cv2.VideoCapture`) to extract:
      - `fps` (float)
      - `frame_count` (int)
      - `width` (int)
      - `height` (int)
      - `duration` (float, calculated as frame_count / fps)
    - Handle invalid/non-existent files by raising FileNotFoundError or returning None. Ensure the OpenCV VideoCapture is released safely in a `try...finally` block.
  </action>
  <verify>
    services\venv\Scripts\python.exe -m unittest services/ai/tests/test_detection.py
  </verify>
  <done>
    Video properties retriever implemented and tested.
  </done>
  <acceptance_criteria>
    - services/ai/detection/video_service.py exists and contains `get_video_properties`
    - get_video_properties returns accurate metadata for a valid video file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Implement Frame Extractor with skipping</name>
  <files>
    services/ai/detection/frame_extractor.py
  </files>
  <read_first>
    services/ai/detection/video_service.py
  </read_first>
  <action>
    Create and implement `services/ai/detection/frame_extractor.py` for efficient frame retrieval:
    - Implement a generator function `extract_frames(video_path: str, skip_interval: int = 5) -> Iterator[Tuple[np.ndarray, int]]` that yields `(frame, frame_index)`.
    - Apply the efficient `cap.grab()` loop technique: skip N-1 frames using `cap.grab()`, then call `cap.read()` to retrieve and decode only the Nth frame.
    - Ensure robust resource release (`cap.release()`) inside a `finally` block.
  </action>
  <verify>
    services\venv\Scripts\python.exe -m unittest services/ai/tests/test_detection.py
  </verify>
  <done>
    Frame extractor with grab-skipping implemented.
  </done>
  <acceptance_criteria>
    - services/ai/detection/frame_extractor.py exists and contains `extract_frames`
    - Frame skipping outputs frames at correct index offsets (e.g. 0, 5, 10...)
  </acceptance_criteria>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify `test_detection.py` runs and all tests for VideoService and FrameExtractor pass successfully.
</verification>

<success_criteria>
- Video metadata retrieval function works correctly.
- Frame extraction generator correctly decodes only the skipped frames.
</success_criteria>

<output>
After completion, create `.planning/phases/02-detection-subsystem-implementation/02-01-SUMMARY.md`
</output>
