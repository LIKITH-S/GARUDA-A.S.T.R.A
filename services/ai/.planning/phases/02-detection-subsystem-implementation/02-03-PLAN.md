---
phase: 02-detection-subsystem-implementation
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - services/ai/detection/preprocessing.py
autonomous: true
requirements:
  - DETC-05
must_haves:
  truths:
    - "preprocessing.py resizes face crop to 112x112 and encodes to JPEG bytes"
    - "Tests in test_detection.py verify the preprocessed image format and dimensions"
  artifacts:
    - path: "services/ai/detection/preprocessing.py"
      provides: "112x112 JPEG encoded face crops ready for ArcFace ingestion"
---

<objective>
Implement image preprocessing to resize crops to exactly 112x112 pixels and output normalized JPEG bytes.

Purpose: Structure crops to match ArcFace embedding model input shape and file loading requirements.
Output: Crop preprocessing and JPEG encoding module.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-detection-subsystem-implementation/02-CONTEXT.md
@.planning/phases/02-detection-subsystem-implementation/02-RESEARCH.md
@.planning/phases/02-detection-subsystem-implementation/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Implement Preprocessing module</name>
  <files>
    services/ai/detection/preprocessing.py
  </files>
  <read_first>
    services/ai/tests/test_detection.py,
    .planning/phases/02-detection-subsystem-implementation/02-CONTEXT.md
  </read_first>
  <action>
    Create and implement `services/ai/detection/preprocessing.py`:
    - Define a function `preprocess_face(crop_frame: np.ndarray, target_size: tuple = (112, 112)) -> bytes` where target_size defaults to (112, 112) matching D-05.
    - Check if the input frame is empty or invalid; if so, return empty bytes.
    - Use OpenCV `cv2.resize(crop_frame, target_size, interpolation=cv2.INTER_AREA)` to resize the face crop.
    - Encode the resized frame as JPEG using `cv2.imencode('.jpg', resized)`.
    - Return the resulting buffer as `bytes`.
    - Wrap the function in try-except to log errors and return empty bytes on failure.
  </action>
  <verify>
    services\venv\Scripts\python.exe -m unittest services/ai/tests/test_detection.py
  </verify>
  <done>
    Face preprocessing and JPG encoding implemented.
  </done>
  <acceptance_criteria>
    - services/ai/detection/preprocessing.py exists and contains `preprocess_face`
    - preprocess_face outputs valid JPEG bytes with 112x112 target size
  </acceptance_criteria>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify `test_detection.py` runs and all tests for Preprocessing pass successfully.
</verification>

<success_criteria>
- Cropped image is resized to 112x112.
- Resized image is encoded into JPEG bytes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-detection-subsystem-implementation/02-03-SUMMARY.md`
</output>
